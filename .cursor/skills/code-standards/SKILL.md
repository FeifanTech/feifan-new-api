---
name: code-standards
description: 提供代码规范与 Code Review 清单，用于编写代码、审查、重构、提交前自检。当用户编写/审查代码、重构、做 Code Review 或提交前自检，或提到「代码规范」「Code Review」「代码审查」「写代码」「重构」时应用。（参考 agentskills.io：what + when + 关键词）
---

# 代码规范

## 触发条件

- 用户提到：代码规范、Code Review、代码审查、写代码、重构、提交前自检、按规范写。
- 用户意图：按规范写代码、做代码审查或自检、希望逐条对照检查项。

## 红旗清单（触达即停，交人决策）

以下为可观察的违规行为；任一条触达须停止，将决策交给人，不自行合理化绕过。

1. **命名与项目约定明显不符（包/类/方法/变量风格不一致或不可读）**  
   常见借口：「临时变量无所谓」「后面会改」。→ 触达即停，交人决策。

2. **公共 API/对外接口无注释或文档说明用途与约束**  
   常见借口：「代码即文档」「先实现再补」。→ 触达即停，交人决策。

3. **使用 AutoCloseable/流/连接等资源未用 try-with-resources 或等效方式关闭**  
   常见借口：「这里不会泄漏」「调用方会关」。→ 触达即停，交人决策。

4. **魔法数字/字符串未提取为常量或配置且无注释说明含义**  
   常见借口：「就这一处用」「先跑通」。→ 触达即停，交人决策。

5. **明显重复代码未抽取复用（同一逻辑多处拷贝）**  
   常见借口：「先复制再抽象」「时间紧」。→ 触达即停，交人决策。

## 通用原则（请按团队实际替换）

1. **可读优先**：命名达意、结构清晰，注释解释「为什么」而非「是什么」。
2. **单一职责**：函数/类职责单一，避免过长函数与上帝类。
3. **错误与边界**：异常与错误路径显式处理，边界条件有测试或说明。
4. **可测试**：核心逻辑可单测，依赖可替换（构造注入/接口）。
5. **与项目一致**：遵循项目既有风格（命名、包结构、框架约定）。

## Code Review 检查清单

- [ ] **命名与约定**：命名是否达意？是否符合项目约定（包结构、框架、命名风格）？
- [ ] **重复与复用**：是否有明显重复？是否可抽取复用？函数/类是否单一职责、避免过长？
- [ ] **异常与日志**：异常与错误路径是否显式处理？日志是否合理（级别、信息量）？
- [ ] **安全与合规**：是否有敏感数据暴露、注入、越权等风险？是否符合合规要求？
- [ ] **可测试性**：关键逻辑是否有测试或可验证方式？依赖是否可替换（构造注入/接口）？
- [ ] **依赖与复杂度**：是否引入不必要的依赖或复杂度？边界条件是否有测试或说明？

**输出要求**：做 Code Review 时逐条对照上述检查项，标明「满足/不满足/建议」，并尽量给出具体代码示例或修改建议。（参考市场：输出明确、可执行）

## 企业级检查清单（可选，按技术栈择一）

- 若项目为 **Java 企业级应用**，请结合 `docs/architecture/JAVA_ENTERPRISE_CHECKLIST.md` 第二、编码与实现检查清单（含 2.6 安全、2.7 并发）逐项审查，并给出结论与建议。
- 若项目为 **Python 企业级应用**，请结合 `docs/architecture/PYTHON_ENTERPRISE_CHECKLIST.md` 第二、编码与实现检查清单（含 2.6 安全、2.7 并发）逐项审查，并给出结论与建议。
- 若项目为 **TypeScript/Node 企业级应用**，请结合 `docs/architecture/TS_ENTERPRISE_CHECKLIST.md` 第二、编码与实现检查清单（含 2.6 安全、2.7 并发）逐项审查，并给出结论与建议。
- 如发现明显严重问题（如 SQL 拼接、日志输出敏感信息），应单独标注为高优先级；非上述技术栈可跳过本段。

## 示例（Java，可按语言替换）

```java
// ✅ 推荐：异常打日志并包装再抛
try {
  return client.call();
} catch (IOException e) {
  log.warn("Call failed", e);
  throw new ServiceException("调用失败", e);
}

// ❌ 避免：吞掉异常
try {
  return client.call();
} catch (IOException e) {}
```

## 与 memory/ 的联动（可选）

- 若当前仓库存在 `memory/` 目录，且本次产出了**已验证的编码约定、命令或目录说明**（如项目命名规范、启动/测试命令），则可追加到 `memory/engineering.md` 的约定区。
- 若本轮任务或会话结束，可将本次摘要追加到 `memory/changelog.md`：日期、本次做了什么、涉及模块或文件、如何验证。
- 若不存在 `memory/`，则跳过本段，不报错。

## 工作结束后的默认动作

- **提交 GitHub**：任务或迭代完成后，默认执行或明确建议用户执行「提交到 GitHub」（`git add` → `git commit` → `git push`），避免工作结束未提交导致无法回滚；commit 信息需清晰、可与需求/任务关联。

## 使用说明

- 本 Skill 可被 **role-developer** 引用：角色 Agent 在写代码/审查时先遵循 development-principles，再应用本规范与检查清单。
- 单独做 Code Review 或写代码时，Agent 可直接匹配本 Skill；与 **development-principles** 一起被 role-developer 串联使用。
- 若希望「打开某类文件就带规范」，可在 `.cursor/rules/` 下增加按 globs 的规则（如 `java-standards.mdc`，globs: `**/*.java`）。
